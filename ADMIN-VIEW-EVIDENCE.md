# الآن يستطيع المدير رؤية روابط شواهد كل معلم

## ما أضفته
في **إدارة المعلمين**، بجانب كل معلم أصبح هناك زر جديد **"عرض الشواهد"**. عند الضغط عليه تظهر نافذة تعرض:
- كل عناصر الأداء الوظيفي التي أضاف لها المعلم شاهداً
- الرابط الذي أضافه المعلم تحت كل عنصر (قابل للضغط لفتحه مباشرة)
- الوصف المختصر الذي كتبه المعلم (إن وُجد)
- تاريخ إضافة كل شاهد

## الملفات التي تحتاج استبدالها
حمّل الملفات الثلاثة المرفقة أعلاه:
- `admin.html` → في جذر المشروع
- `admin.js` → داخل مجلد `js/`
- `style.css` → داخل مجلد `css/`

## ثم ارفعها
```bash
git add .
git commit -m "إضافة عرض شواهد المعلمين للمدير"
git push
```
انتظر دقيقة، وتحقق من Render حتى تظهر `Your site is live`.

## كيف تستخدمها
1. سجّل دخولك كمدير
2. اذهب إلى **إدارة المعلمين**
3. اضغط **"عرض الشواهد"** بجانب أي معلم
4. ستظهر نافذة منظّمة بكل الروابط التي أضافها، مجمّعة حسب عنصر التقييم — اضغط أي رابط لفتحه في تبويب جديد
